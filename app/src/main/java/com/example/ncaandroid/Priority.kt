package com.example.ncaandroid

// This is just an enum class that we will use to represent the priority of a task
enum class Priority {
    HIGH, // 3
    MEDIUM, // 2
    LOW, // 1
    DEFAULT // 0
}